# Profile Directory
Controllers for user profile-related operations.

## Files
### change-color-theme.ts
Updates the authenticated user's color theme preference in the database. Expects `colorTheme` in the request body.
