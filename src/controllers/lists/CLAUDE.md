# Lists Directory
Controllers for fetching static/reference data lists used throughout the application.

## Files
### get-event-categories.ts
Returns all active event categories from the database.

### get-event-types.ts
Returns all active event types from the database.
